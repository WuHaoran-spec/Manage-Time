# Manage Time 隐私说明

生效日期：2026-09-25。适用于本仓库的 0.1.0 预览版。

## 保存哪些数据

授权使用情况访问后，应用保存系统提供的前台应用包名、开始 / 结束时间。可选屏幕观察开启后，仅对手动选择的应用保存采样时间、应用包名、最多 240 字符的标题 / 可见文字线索、候选类型和来源。标签、预算、暂停区间及权限相关开关保存在本机设置中。

不会录制视频、音频、截图，不记录按键或输入事件。输入框和密码节点跳过。已知聊天、支付、密码管理、输入法和系统敏感应用不采集内容。过滤无法承诺识别所有私人内容；已选择应用公开的可见文字仍可能包含个人信息。

## 数据去向

本应用没有互联网访问权限（`INTERNET`），不设置账号、服务器、分析服务或广告 SDK。WorkManager 依赖包含网络状态查询权限，但这不允许把数据传到互联网。记录保存在 Android 应用私有目录中的 SQLite 数据库。系统云备份与设备迁移备份禁用。数据未使用独立数据库加密；其保护依赖 Android 应用隔离和设备存储安全。

手动导出 CSV 时，应用把所选日期的记录写入你在系统文件选择器中指定的位置。该位置可能由云盘或其他文档提供者管理，导出文件的备份和共享由该提供者及你决定。CSV 包含可读的浏览线索，请自行保管。

## 开启、暂停和删除

- 屏幕观察默认关闭，应用选择默认空白；先在应用中同意，再到系统设置授权。
- 只有允许通知并保留“内容记录状态”渠道时才采集屏幕观察，记录状态通知带暂停入口。
- “暂停全部记录”停止采样，并从后续用时导入中排除暂停区间。安卓系统自身的使用记录不受本应用控制。
- 可关闭屏幕观察、取消某个应用的选择，或撤销系统权限，停止新的采集。取消选择不会自动删除已有历史。
- 保留期可选 7 / 30 / 90 天；在应用刷新或后台同步时处理过期记录，调度被系统停止时清理也可能延迟。
- “删除所有本机记录”清除使用与观察数据并设置历史截止线，防止再次导入已删历史。SQLite 开启 secure_delete 并整理数据库；这不是对底层闪存取证清除的保证。
- 删除本机记录不会删除先前导出的 CSV。卸载会删除应用私有数据；外部导出文件仍需自行删除。

## 可见文字的含义

观察只说明采样时某段文字在无障碍节点中可见，不能证明你实际阅读或持续观看了它。标题可能缺失、过期或属于推荐区域。时间轴区分系统使用会话和未经验证的屏幕观察，不生成或补全缺失的观看历史。

## 联系与问题反馈

通过本仓库 Issue 反馈，**不要附上未脱敏的私人时间轴、标题或 CSV**。维护者没有访问你手机记录的渠道。
